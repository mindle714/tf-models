#!/usr/bin/bash

epoch=100000

echo "epoch $epoch"
for eval in *-${epoch}.eval; do 
  suf="$(echo $eval | sed 's/\.eval//g' | cut -d- -f1)"; 
  printf "$suf "; tail -n1 $eval | sed 's/final: //g'
done | awk '{printf "%20s\t%.5f\t%.5f\n", $1, $2, $3}'
